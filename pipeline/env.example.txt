# Copy to .env and fill in. NEVER commit .env.
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
# Once set, NEVER change it: changing the salt breaks all existing joins.
FRAUD_SALT=replace-with-a-long-random-string
