[pytest]
testpaths = .
pythonpath = ../pipeline ../tools ../model
markers =
    slow: integration tests that generate data and run the full pipeline
    security: leakage and PII checks -- never skip these
addopts = -v --strict-markers
